import { Circle } from "better-react-spinkit";

import React from "react";

const Loader = () => {
  return (
    <div className="loader">
      <Circle size={100} color="#28a745" />
    </div>
  );
};

export default Loader;
